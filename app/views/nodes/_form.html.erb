<script type="text/javascript">
  var node_candidates = <%= raw JSON.pretty_generate @node_candidates %>;
</script>

<%= form_for(@node) do |node_form| %>
  <% if @node.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@node.errors.count, "error") %> prohibited this node from being saved:</h2>
    <ul>
    <% @node.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <table>
  <tr>
    <th><%= node_form.label :ip %></th>
    <td><%= node_form.select :ip, @node_candidates.collect{|node_candidate| [node_candidate[:ip], node_candidate[:ip]]}%></td>
  </tr>
  <tr>
    <th><%= node_form.label :name %></th>
    <td><%= node_form.text_field :name, :readonly => true%></td>
  </tr>
  </table> 

  <div class="actions">
    <%= node_form.submit %>
  </div>
<% end %>
