<h1>Current user</h1>

<% current_user.ensure_authentication_token! %>
<table>
<tr>
  <th>Email</th>
  <td><%= current_user.email %></td>
</tr>
<tr>
  <th>Authentication token</th>
  <td>auth_token=<%= current_user.authentication_token %></td>
</tr>
<tr>
  <th>Set up command for dodai-deploy node</th>
  <td>setup-env/setup.sh -s <%= Settings.puppet.server %> -t <%= current_user.authentication_token %> node</td>
</tr>
<tr>
  <th>Set up command for dodai-deploy node via proxy</th>
  <td>setup-env/setup.sh -x PROXY_SERVER -s <%= Settings.puppet.server %> -t <%= current_user.authentication_token %> node</td>
</tr>
</table>
